services:
  airco-api:
    build:
      context: .
      dockerfile: Dockerfile
    container_name: airco-api
    restart: unless-stopped

    ports:
      - "3000:3000"

    env_file:
      - .env

    environment:
      NODE_ENV: production
      PORT: 3000
